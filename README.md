# 선셋 가드 — Frontier Edition v0.3

19세기 미국 서부극에서 출발해 원시 부족, 언데드, 기계 군단, 늪지 생물, 빙결 괴물, 화염 생물, 망령, 공허 생명체까지 전선이 확장되는 가로형 방치 디펜스 게임입니다. 현재 런타임 그래픽은 외부 이미지 없이 Canvas 픽셀 드로잉을 사용합니다.

## 실행

전체 폴더의 `index.html`을 브라우저에서 열어 실행합니다. 폴더 버전은 `assets.js`, `game.js`, `campaign.js`, `boot.js`, `style.css`, `overhaul.css`를 함께 유지해야 합니다. `python3 build.py`를 실행하면 해당 파일들을 내장한 단일 `sunset-guard.html`을 만들 수 있습니다.

## 앱 시작 흐름

앱을 실행하면 전투 화면으로 바로 진입하지 않고 전용 로딩/타이틀 화면을 표시합니다.

- 신규 플레이어: `게임 시작`
- 기존 플레이어: 저장된 현재 웨이브를 표시하고 `WAVE N 계속하기`
- 최고 도달 웨이브와 현재 지역을 시작 화면에서 확인
- 시작 버튼을 누르기 전에는 전투 루프 정지
- 시작 후 현재 저장 웨이브의 처음부터 방어전 재개

진행 기록은 현재 브라우저의 로컬 저장소에 저장됩니다. 서버 계정과 클라우드 저장은 아직 구현되어 있지 않습니다.

## 5,000 웨이브 캠페인

캠페인은 총 **5,000 웨이브**입니다.

- 100웨이브 = 1개 지역(Chapter)
- 500웨이브 = 1개 대지역(Act)
- 총 50개 Chapter / 10개 Act
- 10웨이브마다 Elite
- 100웨이브마다 Region Boss
- 500웨이브마다 Act Boss
- 5,000웨이브는 최종 결전

현재 Act 구성은 다음과 같습니다.

1. 개척지의 야수
2. 잊힌 원시 부족
3. 죽은 자의 변경
4. 강철 군단
5. 진홍 늪지
6. 빙결 전선
7. 잿불 황무지
8. 월광 황야
9. 공허의 균열
10. 최후의 지평선

각 Act에는 5개 지역과 일반 몬스터 3계열, 엘리트, 보스 이름 및 난이도 메타데이터가 정의되어 있습니다. 1차 단계에서는 기존 픽셀 적 렌더러를 재사용하며, 다음 단계에서 Act별 몬스터 외형과 행동 패턴을 확장합니다.

## 현재 게임 시스템

- 영웅 / 마을 강화 / 장비 / 전투 배치 / 상점 / 설정 6개 관리 메뉴
- 보유 영웅 전투력순 정렬 및 미보유 영웅 잠금 표시
- 영웅 상세: 성장 / 장착 장비 / 능력치
- 영웅별 개별 강화와 마을 강화 4종
- 탱커 철퇴 근접 공격: 주 대상 100%, 범위 내 다른 적 20%
- 근거리 무기 유형 제한
- 최대 5명 수비대 배치 및 전투 중 슬롯 교환
- 영웅 모집 / 장비 상자 / 10회 이내 미보유 영웅 확정
- 배경음 / 효과음 개별 볼륨
- 손상 저장 방지, 백업, v1 → v2 이전, 중복 배치/장비 검증, 다중 창 저장 충돌 감지

## 픽셀 프로토타입

`assets.js`에서 전장, 영웅, 적, 장비, 마을 화면을 Canvas 픽셀 도형으로 그립니다. 기존 `assets/`의 HD 이미지 파일은 보관용이며 현재 게임에서 직접 참조하지 않습니다.

기본 여성 총잡이 장수연과 여섯 영웅의 역할 구분, 왼쪽 최대 5명 수비대, 오른쪽에서 진입하는 적이라는 전투 방향은 유지합니다.

## 저장 / 계정 / 소리

저장 키는 `sunset-guard-v2`입니다. v1 기록을 자동 이전하며 내보낸 저장파일을 다시 불러올 수 있습니다. 다른 브라우저나 기기로 옮길 때는 저장파일 내보내기/불러오기를 사용해야 합니다.

계정 연동 버튼은 현재 미지원 안내만 표시합니다. 인증 서버, 클라우드 저장, 결제는 아직 구현되어 있지 않습니다. Web Audio로 간단한 배경음과 효과음을 생성하며 첫 사용자 입력 후 활성화됩니다.

## 테스트

`npm install playwright` 후 `npx playwright install chromium`, `node test.cjs`로 브라우저 통합 테스트를 실행할 수 있습니다. 테스트에는 기존 영웅/장비/저장/보스 로직과 함께 앱 시작 화면, 10개 Act, 50개 Chapter, 5,000웨이브 최종 지역 메타데이터 검증이 포함됩니다.

## 전면 보완 계획

전체 보완은 한 번에 덮어쓰지 않고 기능 단위로 진행합니다. 세부 순서와 완료 기준은 `OVERHAUL_ROADMAP.md`를 확인하세요.
