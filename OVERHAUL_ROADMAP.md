# 서부마을 지키기 전면 보완 로드맵

한 번에 전체 구조를 덮어쓰지 않고 최신 `main`을 기준으로 기능 단위로 나눠 적용합니다. 각 단계는 이전 단계의 저장 데이터와 플레이 흐름을 유지하는 것을 원칙으로 합니다.

## 1차 — 앱 시작 경험 + 5,000 웨이브 기반 ✅ 적용

- 전용 로딩/타이틀 화면
- 신규 `게임 시작`, 기존 `WAVE N 계속하기`
- 시작 전 전투 정지
- 총 5,000웨이브 / 50 Chapter / 10 Act
- 10웨이브 Elite / 100웨이브 Region Boss / 500웨이브 Act Boss

## 2차 — 몬스터/보스/지역 콘텐츠 확장 ✅ 적용

- 10개 Act별 배경/적 실루엣/팔레트
- 신속·돌진·중장갑·괴력·원거리·지원·광폭·위상·증식 적 행동
- 보스 광역 돌진 / 소환·회복 / 보호막·회복 패턴
- 보스 이름·등급·체력·보호막 UI

## 3차 A — 첨부 기준 7인 영웅/배치/8부위 장비 ✅ 적용

- 낸시 / 루나 / 엘로나 / 제시 / 줄리 / 첼리 / 티나
- 기존 내부 ID를 유지해 v2 전투/저장 호환
- v3 저장 `sunset-guard-v3-seven` + v2 그림자 저장
- 모자 / 한벌 옷 / 신발 / 무기 / 목걸이 / 반지1 / 반지2 / 뱃지
- 기존 `armor/charm` 자동 이전
- 1 / 10 / 100회 묶음 강화
- 추천 배치 / 최초 보스 보상 / 체크포인트

## 3차 B — 성장·경제·장기 플레이 ✅ 적용

- 5,000웨이브 HP/공격력 성장곡선 재조정
- 영웅/마을 Lv.500 장기 강화비용 재설계
- 일반 / 고급 / 희귀 / 영웅 / 전설 장비 등급
- 장비 +0 ~ +20 강화
- 골드 + `부품` 강화 재화
- 일반/Elite/Region Boss/Act Boss 장비 드랍 차등
- 체크포인트 반복 파밍, 본 진행 웨이브 별도 보존
- 일일 / 주간 목표
- 웨이브·보스·강화·7인 모집 업적
- 경제/미션/장비 메타데이터를 v3 진행 데이터에 저장

## 4차 — 모바일/PWA 플레이 경험 ✅ 적용

- 기존 `mobile-test-ready`를 통째로 병합하지 않고 필요한 기능만 최신 코드에 재작성
- 터치 기기는 항상 논리 가로 좌표 사용
- 물리 세로 화면에서도 별도 세로 UI로 바꾸지 않고 게임 전체를 90° 회전
- 지원 브라우저에서는 첫 사용자 입력 이후 fullscreen + landscape orientation lock 시도
- 지원하지 않는 브라우저에서는 CSS 회전으로 계속 플레이
- 전투 중 상단 SUNSET GUARD 헤더 제거 후 전장 높이 확대
- 5명 수비대 슬롯을 한 화면 한 줄에 유지
- safe-area 대응
- 긴 영웅/장비/경제/미션 화면은 팝업 내부만 스크롤
- PWA `manifest.webmanifest`, SVG 앱 아이콘, Service Worker 추가
- PWA는 fullscreen / landscape 실행 요청
- 최신 Phase 3B / Phase 5 파일까지 오프라인 캐시

## 5차 — 전투 UX·연출·성능 ✅ 적용

- 기존 Canvas 픽셀 방향을 유지한 절차형 영웅 비전투 모션
- 공격 시 반동/근접 동작 변화
- 적 이동 bob / 보스 강조 동작
- 총구 화염, 피격 파편, 치명타 강조
- 필살기 빔/링 연출
- 보스 등장 시 이름/등급 시네마틱 배너
- 화면 흔들림은 기본 OFF, 설정에서 선택 사용
- 자동 / 높음 / 중간 / 낮음 품질 설정
- FPS와 전장 부하에 따른 자동 이펙트 수 조절
- 저품질 상태에서는 파티클과 동시 이펙트 수 제한
- 전투 계산식과 저장 진행 자체는 연출 레이어와 분리

## 6차 — 전체 검증·상용화 준비 🔄 진행 중

### 검증 코드 준비 ✅

- `test-syntax.cjs` — 전체 JS 구문 검사
- `test.cjs` — 기존 전투 + 5,000웨이브 캠페인
- `test-phase3.cjs` — 7인 / 8부위 / v3-v2 저장
- `test-phase3b.cjs` — 성장곡선 / 장비 강화 / 미션 / 파밍
- `test-mobile.cjs` — 세로→가로 회전 / 5인 슬롯 / PWA / 모바일 모달
- `test-phase5.cjs` — 보스 등장 / 필살기 FX / 품질 설정
- `test-release.cjs` — 위 검증을 묶는 릴리즈 진입점

### 단일 빌드 검증 준비 ✅

`test-release.cjs`에서 `build.py`로 임시 단일 HTML을 생성하고 다음을 검사하도록 구성했습니다.

- 최신 Phase 3B / Phase 5 / 모바일 코드가 단일 HTML에 포함
- 외부 `<script src>` 제거
- 외부 stylesheet 링크 제거
- 릴리즈 필수 파일 존재 확인

### 실제 실행 필요 ⏳

현재 GitHub 연결 환경에서는 Playwright Chromium이나 실제 모바일 기기를 직접 실행하지 못했으므로 아래는 병합 전 실제 검증이 필요합니다.

- `node test-release.cjs`
- Android 실제 기기 세로/가로/전체화면
- iPhone Safari 세로/가로/PWA
- 백그라운드 복귀
- WAVE 100 / 500 / 1,000 / 2,500 / 5,000
- v1 / v2 / v3 저장 이전과 손상 복구
- 체크포인트 파밍 중 종료/재접속
- 날짜 경계에서 일일/주간 초기화
- 저사양 기기 자동 품질 전환

## 서버 상용화 단계 — 별도

현재 로컬 프로토타입을 실제 서비스 경제로 전환하려면 다음이 별도 필요합니다.

- 실제 계정 인증
- 클라우드 저장과 다중 기기 충돌 처리
- 서버 권위 재화 / 영웅 모집 / 장비 드랍
- 결제 및 영수증 검증
- 계정 삭제 / 개인정보 처리
- 서버 기반 오프라인 보상

## 현재 상태

**1차 → 5차 코드는 적용됐고, 6차 검증 코드와 실행 절차까지 준비했습니다.**

PR은 실제 신규 Playwright 회귀 테스트 및 모바일 실기기 테스트 전까지 **Draft 유지**합니다. `main`에는 아직 병합하지 않습니다.
